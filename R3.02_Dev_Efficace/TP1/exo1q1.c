#include <stdlib.h>
#include <stdio.h>

int main(){
    int x = 100;
    itn y = 30;
    p=(int*)
    // definition d'un pointeur
    int *pt *pt2;
    // affectation de la valeur de x au pointeur pt 
    pt = &x;
    y = *pt;
    *pt = 50;
    pt2 = pt;

    //*pt = manière d'appeler le pointeur
    printf("\nla valeur est de : %d\n\n", *pt);


    
    
}