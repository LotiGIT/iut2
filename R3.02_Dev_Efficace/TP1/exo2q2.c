#include <stdlib.h>
#include <stdio.h>
#include <string.h>

typedef char chaine20[21];

typedef struct{
    chaine20 nom;
    int population;
    chaine20 capitale;
} t_region;
t_region* P1;

int main(){
    t_region*region(pt_region);
    region(pt_region) = (t_region*)malloc(sizeog(t_region));

}
